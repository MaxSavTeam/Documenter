<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/bottom_cycle_height"
        android:focusable="false"
        android:scrollbarSize="@dimen/scrollbars_size"
        android:scrollbarStyle="insideInset"
        android:scrollbars="vertical"
        tools:ignore="UselessParent">

        <com.maxsavitsky.documenter.editor.TextEditor
            android:id="@+id/edittextEntry"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:background="@null"
            android:ems="10"
            android:gravity="start|top"
            android:hint="@string/enter_your_minds_here"
            android:inputType="textMultiLine"
            android:scrollbars="vertical"
            android:textIsSelectable="true"
            android:textSize="22sp" />

    </ScrollView>

</LinearLayout>
